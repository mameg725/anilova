<%= form_for(message, url:"/admin/messages") do |f| %>
	<%= f.label "Title" %>
	<%= f.text_field :title %>
	<%= f.label "Text" %>
	<%= f.text_field :news_text %>
	<%= f.hidden_field :user_id, :value => @user.id %>
	<%= f.submit "Sent" %>
<% end %>