<%= render "layouts/leftbar", me: current_user %>
<div class="col-xs-9">
	<%= render "public/posts/post_form", post: @post %>
	<%= render "public/users/menubar", me: current_user %>


	<% if @user.favorites.count > 0 %>
		<%= @favorites.each do |favorite| %>
			<table>
				<tbody>
					<tr>
						<td rowspan="5">
							<%= link_to(favorite.post) do %>
								<%= attachment_image_tag(favorite.post, :post_image, :fill, 200, 200, fallback: "カメラでの撮影禁止アイコン 3.jpeg") %>
							<% end %>
						</td>
					</tr>
					<tr>
						<td>
							<%= link_to(favorite.post.user) do %>
								<%= attachment_image_tag(favorite.post.user, :icon_image, :fill, 50, 50, fallback: "カメラでの撮影禁止アイコン 3.jpeg", size:"50x50") %>
								<%= favorite.post.user.name %>
								@<%= favorite.post.user.friendly_url %>
							<% end %>
						</td>
					</tr>
					<tr>
						<td>
							<%= link_to(favorite.post) do %>
								<%= favorite.post.murmur %>
							<% end %>
						</td>
					</tr>
					<tr>
						<td>
							<% favorite.post.tag_list.each do |tag| %>
								<span><%= tag %></span>
							<% end %>
						</td>
					</tr>
					<tr>
						<td><%= favorite.post.created_at %></td>
						<td>
							<% if favorite.post.favorite_by?(current_user) %>
								<%= link_to post_favorites_path(favorite.post), method: :delete do %>
									<span class="glyphicon glyphicon-heart">
										<%= favorite.post.favorites.count%>
									</span>
								<% end %>
							<% else %>
								<%= link_to post_favorites_path(post), method: :post do %>
									<span class="glyphicon glyphicon-heart-empty">
										<%= favorite.post.favorites.count %>
									</span>
								<% end %>
							<% end %>
						</td>
						<td>
							<%= link_to "削除", post_path(favorite.post), method: :delete %>
						</td>
					</tr>
				</tbody>
			</table>
		<% end %>
	<% else %>
		<p>Let's submit !</p>
	<% end %>
</div>